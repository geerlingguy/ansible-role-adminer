---
- hosts: localhost
  remote_user: root

  vars:
    adminer_install_dir: /opt/adminer
    adminer_add_apache_config: false
    apache_mods_enabled:
      - rewrite.load

  roles:
    - ansible-role-adminer
